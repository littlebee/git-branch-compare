{
  "name": "git-branch-compare",
  "main": "./lib/git-branch-compare",
  "version": "0.0.0",
  "description": "Compares two git branches and shows results in file tree and differences between files using split-diff",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "git-branch-compare:toggle"
  },
  "repository": "https://github.com/atom/git-branch-compare",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "atom-package-deps": "^4.3.1",
    "disposable-event": "^1.0.3",
    "marked": "^0.3.9",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  }
}
