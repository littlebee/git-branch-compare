'use babel';

import GitBranchCompareView from '../lib/git-branch-compare-view';

describe('GitBranchCompareView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
